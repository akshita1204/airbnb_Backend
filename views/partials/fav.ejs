<form action="/fav-list" method="post" class="mt-2">
    <input type="hidden" name="id" value="<%= home.id %>">
    <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-500 transition duration-300">
      Add to Favourites
    </button>
  </form>